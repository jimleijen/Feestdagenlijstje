# In Expo Go on a physical device, "localhost" means the phone itself — use your
# machine's LAN IP (e.g. http://192.168.1.23:4000) so the app can reach the local server.
EXPO_PUBLIC_API_URL=http://localhost:4000
