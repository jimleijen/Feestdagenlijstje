import { createDrawRouter } from "./draws";

// Plain name-drawing for surprises etc. — no hints/messages feature, that's Secret Santa's thing.
export const lootjesRouter = createDrawRouter("LOOTJES");
